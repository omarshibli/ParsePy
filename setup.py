from setuptools import setup, find_packages

setup(
    name='parsepy',
    version='1.0.0',
    description='ParsePy is a Python client for the Parse REST API',
    long_description=open("README.mkd").read(),
    packages=find_packages(),
    author='omarshibli',
    author_email='',
    url="https://github.com/omarshibli/ParsePy.git",
    license="GPL",
    )

